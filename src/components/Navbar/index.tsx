import Navigation from '../../common/Navigation';
import './Navbar.scss';

const Navbar = () => {
  return (
    <nav className="navbar">
      <Navigation />
    </nav>
  );
};

export default Navbar;
