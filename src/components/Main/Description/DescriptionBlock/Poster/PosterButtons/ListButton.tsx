import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const ListButton = () => {
  return <FontAwesomeIcon className="list-button" icon={['fas', 'list']} />;
};

export default ListButton;
