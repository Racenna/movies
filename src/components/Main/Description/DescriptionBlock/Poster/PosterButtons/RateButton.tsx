import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const RateButton = () => {
  return <FontAwesomeIcon className="rate-button" icon={['fas', 'star']} />;
};

export default RateButton;
